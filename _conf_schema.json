{
  "output_format": {
    "description": "输出形式",
    "type": "string",
    "hint": "pdf=发送PDF文件；image=转成图片(第1页预览)；text=仅发送文字与下载链接",
    "options": ["pdf", "image", "text"],
    "default": "pdf"
  },
  "stop_on_failure": {
    "description": "下载失败即停止",
    "type": "bool",
    "hint": "开启后，某一张卷下载失败时立即停止后续下载",
    "default": true
  },
  "timeout": {
    "description": "下载超时（秒）",
    "type": "int",
    "hint": "单个请求的超时时间，单位秒",
    "default": 60
  },
  "source": {
    "description": "下载源",
    "type": "string",
    "hint": "xtremepapers=默认源；custom=使用下方自定义API",
    "options": ["xtremepapers", "custom"],
    "default": "xtremepapers"
  },
  "custom_api_base": {
    "description": "自定义 API 地址",
    "type": "string",
    "hint": "留空则使用默认源。需与默认源目录结构一致（含 CAIE/{等级}/{科目}/{文件} 及 index.php 列表接口）",
    "default": ""
  }
}
